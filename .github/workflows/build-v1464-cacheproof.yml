name: Legacy v14.6.4 recovery (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy notice
        run: echo "v14.6.4 recovery is retired. Current production release is v14.6.7."
