name: Validate search startup
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: JavaScript syntax
        run: |
          node --check app.js
          node --check sw.js
          node --check sedori-ai.user.js
          node --check test-v1460.js
      - name: Critical search wiring
        shell: bash
        run: |
          grep -F 'function start()' app.js
          grep -F 'bulkSearchBtn' app.js
          grep -F "cmd('search'" app.js
          grep -F 'sedoriBridgeCommand' sedori-ai.user.js
          grep -F 'sedoriBridgeResult' sedori-ai.user.js
          grep -F 'app.js?v=1460' index.html
      - name: Functional smoke test
        run: node test-v1460.js
      - name: Version consistency
        shell: bash
        run: |
          python - <<'PY'
          import re, pathlib
          app=pathlib.Path('app.js').read_text()
          idx=pathlib.Path('index.html').read_text()
          sw=pathlib.Path('sw.js').read_text()
          av=re.search(r"VERSION='([^']+)'", app)
          iv=re.search(r'せどりAI <small[^>]*>v([^<]+)</small>', idx)
          if not av or not iv:
              raise SystemExit('version not found')
          if av.group(1) != iv.group(1):
              raise SystemExit(f'version mismatch: app={av.group(1)} index={iv.group(1)}')
          if "sedori-ai-v1460" not in sw:
              raise SystemExit('service worker cache mismatch')
          print('version OK:', av.group(1))
          PY
